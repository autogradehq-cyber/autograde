import Seo from "@/components/Seo";
import { Card, CardContent, CardHeader, CardTitle } from "@/components/ui/card";
import { Button } from "@/components/ui/button";
import { Badge } from "@/components/ui/badge";

type Pick = {
  name: string;
  score: number;        // AutoGrade Score 0–100
  pros: string[];
  cons: string[];
  merchantUrl: string;
};

const PICKS: Pick[] = [
  {
    name: "BeamPro H11 LED (2025)",
    score: 92,
    pros: ["Sharp cutoff", "Low glare", "Compact housing"],
    cons: ["Higher price"],
    merchantUrl: "https://example.com/beampro-h11",
  },
  {
    name: "NightLaser H11 MAX",
    score: 88,
    pros: ["Great thermal control", "Plug-and-play"],
    cons: ["Bulky driver"],
    merchantUrl: "https://example.com/nightlaser-h11-max",
  },
  {
    name: "ValueBright H11",
    score: 82,
    pros: ["Great value", "Decent beam"],
    cons: ["Average longevity"],
    merchantUrl: "https://example.com/valuebright-h11",
  },
];

export default function H11Category() {
  return (
    <main className="max-w-6xl mx-auto px-4 py-10">
      <Seo
        title="Best H11 LED Headlight Bulbs — AutoGrade"
        description="Top H11 upgrades ranked by AutoGrade Score™ using fitment confidence, return risk, and real-world performance."
        url="https://autogradehq.com/lighting/h11"
      />
      <h1 className="text-3xl font-bold mb-2">Best H11 LED Headlight Bulbs</h1>
      <p className="text-slate-400 mb-8">
        Rankings based on AutoGrade Score™ (fitment confidence, return risk, performance).
      </p>

      <div className="grid md:grid-cols-3 gap-6">
        {PICKS.map((p) => (
          <Card key={p.name} className="bg-slate-900 border-slate-800">
            <CardHeader>
              <CardTitle className="flex items-center justify-between">
                <span>{p.name}</span>
                <Badge className="bg-cyan-500 text-slate-900">AG {p.score}</Badge>
              </CardTitle>
            </CardHeader>
            <CardContent className="space-y-3">
              <div>
                <div className="text-sm text-slate-400">Pros</div>
                <ul className="list-disc list-inside text-sm">
                  {p.pros.map((x) => <li key={x}>{x}</li>)}
                </ul>
              </div>
              <div>
                <div className="text-sm text-slate-400">Cons</div>
                <ul className="list-disc list-inside text-sm">
                  {p.cons.map((x) => <li key={x}>{x}</li>)}
                </ul>
              </div>
              <Button asChild className="w-full bg-cyan-500 text-slate-900 hover:bg-cyan-400">
                <a href={p.merchantUrl} target="_blank" rel="noopener noreferrer">See Best Price</a>
              </Button>
            </CardContent>
          </Card>
        ))}
      </div>
    </main>
  );
}
